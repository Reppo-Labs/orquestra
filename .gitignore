node_modules/
dist/
*.tsbuildinfo
# Secrets & local node state — NEVER commit
.env
.env.*
!.env.example
data/
# Live node state: ledger, vote-state, snapshots, and the activity log whose
# error lines echo CLI command output. Never commit operational data.
orquestra-data/
orquestra-data.bak-*/
*.key
# Local dashboard screenshots / browser-automation artifacts
.playwright-mcp/
*.png
# Local agent tooling state — machine-specific, never ship
.claude/
.agents/
skills-lock.json
session-report*.html
# OS / editor
.DS_Store
.idea/
.vscode/
*.log
